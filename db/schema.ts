import {sqliteTable,text,integer,index,uniqueIndex} from 'drizzle-orm/sqlite-core';
export const envelopes=sqliteTable('envelopes',{id:text('id').primaryKey(),owner:text('owner').notNull(),title:text('title').notNull(),status:text('status').notNull(),revision:integer('revision').notNull(),data:text('data').notNull(),updated:text('updated').notNull()},t=>[index('idx_envelopes_owner_updated').on(t.owner,t.updated)]);
export const files=sqliteTable('files',{id:text('id').primaryKey(),owner:text('owner').notNull(),name:text('name').notNull(),digest:text('digest').notNull(),pages:text('pages').notNull(),size:integer('size').notNull()},t=>[index('idx_files_owner').on(t.owner)]);
export const members=sqliteTable('members',{id:text('id').primaryKey(),envelope:text('envelope').notNull(),email:text('email').notNull(),role:text('role').notNull()},t=>[uniqueIndex('idx_members_envelope_email').on(t.envelope,t.email),index('idx_members_email').on(t.email)]);
export const comments=sqliteTable('comments',{id:text('id').primaryKey(),envelope:text('envelope').notNull(),author:text('author').notNull(),name:text('name').notNull(),body:text('body').notNull(),created:text('created').notNull(),resolved:integer('resolved').notNull().default(0)},t=>[index('idx_comments_envelope_created').on(t.envelope,t.created)]);
export const presence=sqliteTable('presence',{id:text('id').primaryKey(),envelope:text('envelope').notNull(),name:text('name').notNull(),seen:integer('seen').notNull()},t=>[index('idx_presence_envelope_seen').on(t.envelope,t.seen)]);
export const contacts=sqliteTable('contacts',{id:text('id').primaryKey(),owner:text('owner').notNull(),name:text('name').notNull(),email:text('email').notNull(),company:text('company').notNull()},t=>[index('idx_contacts_owner').on(t.owner)]);
export const profiles=sqliteTable('profiles',{id:text('id').primaryKey(),data:text('data').notNull()});
